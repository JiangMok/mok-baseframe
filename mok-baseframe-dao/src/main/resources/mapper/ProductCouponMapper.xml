<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mok.baseframe.dao.ProductCouponMapper">

    <resultMap id="BaseResultMap" type="com.mok.baseframe.entity.ProductCouponEntity">
        <id column="id" property="id" />
        <result column="product_id" property="productId" />
        <result column="coupon_id" property="couponId" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, product_id, coupon_id, create_time
    </sql>

    <insert id="insertBatch" parameterType="list">
        INSERT INTO product_coupon
        (
        id,
        product_id,
        coupon_id,
        create_time
        )
        VALUES
        <foreach collection="list" item="productCouponList" separator=",">
            (
            #{productCouponList.id},
            #{productCouponList.productId},
            #{productCouponList.couponId},
            #{productCouponList.createTime}
            )
        </foreach>
    </insert>
    <delete id="deleteByProductId">
        DELETE
        FROM
            product_coupon
        WHERE
            product_id = #{productId}
    </delete>
</mapper>